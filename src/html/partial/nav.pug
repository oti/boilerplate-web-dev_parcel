-
  const getPath = () => {
    let condition = depth;
    let dir = ['./'];
    while(condition) {
      dir.push('../');
      condition--;
    }
    return dir.join('');
  }
nav.Nav
  ul.__list
    li
      a(href=`${getPath()}index.pug`) index
    li
      a(href=`${getPath()}about.pug`) abount
    li
      a(href=`${getPath()}blog/index.pug`) blog
    li
      a(href=`${getPath()}blog/tag1/index.pug`) blog - Tag1
